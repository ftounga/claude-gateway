apiVersion: v1
kind: Service
metadata:
  name: corporate-site
  labels:
    app: corporate-site
spec:
  type: ClusterIP
  selector:
    app: corporate-site
  ports:
    - name: http
      port: 80
      targetPort: http
