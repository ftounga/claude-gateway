# F-111 / SF-111-01 — Version de construction du runner, filtrée par Maven.
# La gateway relit ce fichier DANS le jar qu'elle sert pour savoir quelle version elle propose.
version=${project.version}
stamp=${runner.build.stamp}
commit=${runner.commit}
contract=${runner.contract}
java=${maven.compiler.release}
