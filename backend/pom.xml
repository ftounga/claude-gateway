<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>fr.claudegateway</groupId>
	<artifactId>claude-gateway-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>claude-gateway-backend</name>
	<description>Chat LLM proxy (claude-gateway)</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<jjwt.version>0.12.5</jjwt.version>
		<stripe.version>26.3.0</stripe.version>
		<aws.sdk.version>2.29.29</aws.sdk.version>
		<openpdf.version>1.3.43</openpdf.version>
		<commonmark.version>0.24.0</commonmark.version>
		<!-- SDK Java officiel MCP (F-112 / SF-112-01). Ligne 0.x la plus récente qui prend en charge la
		     révision de protocole 2025-11-25 (négociée en repli vers 2025-06-18…2024-11-05). La révision
		     2026-07-28 exige la ligne 2.x, non retenue pour la fondation — voir ADR-020. -->
		<mcp.sdk.version>0.18.4</mcp.sdk.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<!-- AWS SDK v2 BOM : aligne les versions des modules AWS (Textract, S3) — OCR F-05. -->
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${aws.sdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>
		<!-- OAuth 2.1 pour les clients MCP (F-112 / SF-112-02, ADR-020). L'application devient
		     serveur de ressources (validation des jetons d'accès sur /api/mcp) ET serveur
		     d'autorisation (Spring Authorization Server : /oauth2/*, /.well-known/*). Confiné au
		     package fr.claudegateway.mcp ; la chaîne principale et le login Google sont inchangés. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-authorization-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- WebSocket (F-38 / SF-38-02) : canal du runner /runner/ws. Confiné à
		     fr.claudegateway.runner.channel (handshake authentifie par jeton runner, heartbeat). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-websocket</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<!-- F-110 / SF-110-02 — Markdown → HTML sobre des courriels du client. Sans dépendance transitive ;
		     le HTML brut est échappé et les liens assainis à la configuration du rendu. -->
		<dependency>
			<groupId>org.commonmark</groupId>
			<artifactId>commonmark</artifactId>
			<version>${commonmark.version}</version>
		</dependency>
		<dependency>
			<groupId>org.commonmark</groupId>
			<artifactId>commonmark-ext-gfm-tables</artifactId>
			<version>${commonmark.version}</version>
		</dependency>
		<dependency>
			<groupId>org.liquibase</groupId>
			<artifactId>liquibase-core</artifactId>
		</dependency>

		<!-- Serveur MCP (F-112 / SF-112-01) : SDK Java officiel + transport Streamable HTTP sur
		     Spring MVC. Amène mcp-core, mcp-json et reactor-core en transitif. Confiné au package
		     fr.claudegateway.mcp (le domaine ne dépend jamais du SDK). -->
		<dependency>
			<groupId>io.modelcontextprotocol.sdk</groupId>
			<artifactId>mcp-spring-webmvc</artifactId>
			<version>${mcp.sdk.version}</version>
		</dependency>
		<!-- Implémentation Jackson du mapper JSON et du validateur de schéma du SDK MCP (chargée par
		     ServiceLoader). mcp-core seul n'embarque aucun mapper concret. -->
		<dependency>
			<groupId>io.modelcontextprotocol.sdk</groupId>
			<artifactId>mcp-json-jackson2</artifactId>
			<version>${mcp.sdk.version}</version>
		</dependency>

		<!-- JWT (JJWT) : émission / parsing / validation HS256 -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- OpenPDF (export F-14) : génération PDF pure Java, sans dépendance native.
		     Confinée à fr.claudegateway.export.PdfExporter (le domaine n'en dépend jamais). -->
		<dependency>
			<groupId>com.github.librepdf</groupId>
			<artifactId>openpdf</artifactId>
			<version>${openpdf.version}</version>
		</dependency>

		<!-- Stripe (billing F-09) : SDK officiel — checkout + vérification de signature webhook -->
		<dependency>
			<groupId>com.stripe</groupId>
			<artifactId>stripe-java</artifactId>
			<version>${stripe.version}</version>
		</dependency>

		<!-- AWS Textract + S3 (OCR F-05) : implémentation du provider OCR uniquement.
		     Le domaine ne dépend jamais de ces SDK (Provider Independence) ; ils sont
		     confinés à fr.claudegateway.ocr.provider.TextractOcrProvider. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>textract</artifactId>
		</dependency>
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
		</dependency>
		<!-- AWS STS : requis par la chaîne de credentials par défaut pour échanger le token
		     web-identity de l'IRSA (AssumeRoleWithWebIdentity) contre des credentials temporaires.
		     Sans ce module, WebIdentityTokenCredentialsProvider échoue et AUCUN service AWS
		     (Textract, S3, KMS) ne peut s'authentifier en cluster. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>sts</artifactId>
		</dependency>
		<!-- AWS KMS : envelope encryption des clés API BYOK (F-03, OQ-06). Confiné à
		     fr.claudegateway.byok.KmsEnvelopeCipher (le domaine ne dépend jamais du SDK). -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>kms</artifactId>
		</dependency>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- Driver PostgreSQL. Portee `compile` (et non `runtime`) car PgNotifyRunnerRegistry
		     (F-38 / SF-38-02) utilise l'API LISTEN/NOTIFY du driver (PGConnection/PGNotification).
		     Cet usage reste confine a fr.claudegateway.runner.channel. -->
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Testcontainers (F-100 / SF-100-08) : le verrou « une synchro a la fois par poste » repose sur une
		     mise a jour conditionnelle PostgreSQL ; on la prouve sur un PostgreSQL REEL (image pgvector, la
		     migration 002 exige l'extension). Versions gerees par le BOM Spring Boot. Le test s'ignore
		     proprement sans demon Docker (@Testcontainers(disabledWithoutDocker = true)). -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- Tests en parallèle (2026-09-10). La suite dépassait 4 minutes et elle est
					     rejouée à chaque subfeature : c'était le premier poste de temps d'une
					     livraison. `forkCount` en fraction des cœurs plutôt qu'un nombre fixe — la
					     machine de développement et celle d'intégration n'ont pas la même taille.
					     `reuseForks` conservé : sans lui, chaque classe repaierait le démarrage du
					     contexte Spring, et le parallélisme coûterait plus qu'il ne rapporte. -->
					<forkCount>0.5C</forkCount>
					<reuseForks>true</reuseForks>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
