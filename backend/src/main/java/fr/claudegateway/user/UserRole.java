package fr.claudegateway.user;

/**
 * Rôle applicatif d'un utilisateur. {@code USER} par défaut ;
 * {@code ADMIN} réservé aux fonctions d'administration (features ultérieures).
 */
public enum UserRole {
    USER,
    ADMIN
}
